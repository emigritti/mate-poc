# Nginx gateway — routing centralizzato su porta 8080
# nginx.conf è montato come /etc/nginx/conf.d/default.conf (dentro il contesto http{})
FROM nginx:alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
